version: 0.1
os: linux
files:
  - source: /
    destination: /var/www/html/news
    overwrite: true
hooks:
  ApplicationStart:
    - location: scripts/start_server.sh
      timeout: 300
      runas: ubuntu
